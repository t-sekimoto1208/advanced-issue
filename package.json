{
  "name": "advanced-issue",
  "version": "1.0.0",
  "description": "問い合わせ管理アプリ",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "jest",
    "test:ci": "jest --coverage --ci",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.3.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
